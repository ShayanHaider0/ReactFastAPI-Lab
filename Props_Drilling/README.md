# React Props Drilling Project

This project is a beginner-friendly ReactJS application built to understand **Props Drilling**.

It displays multiple job cards by passing data from a parent component (`App.jsx`) to a reusable child component (`Card.jsx`) using props.

## 🚀 Tech Stack
- ReactJS
- JavaScript (ES6)
- CSS
- Vite

## 📚 Key Learning
- Props drilling in React
- Component-based architecture
- Dynamic rendering using `map()`

## ▶️ Run Locally
```bash
npm install
npm run dev
